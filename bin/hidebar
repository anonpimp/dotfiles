#!/bin/bash 

toggle_bar() {
  if [[ -z $(eww windows | grep '*bar') ]]; then
    eww open bar
  else
    eww close bar
  fi
}

if [[ -z $(pidof eww) ]]; then
  eww daemon
  toggle_bar
else
  toggle_bar
fi
